import { number, minLength, Input, stringAsync, objectAsync } from "valibot";

export const ${props}Schema = objectAsync({
    id: number(),
    name: stringAsync("Este campo es obligatorio", [minLength(3)]),
    slug: stringAsync("Este campo es obligatorio", [minLength(3)]),
});

export type ${Props}Schema = Input<typeof ${props}Schema>;
export type ${Props}EntitySchema = Omit<${Props}Schema, "id">;
