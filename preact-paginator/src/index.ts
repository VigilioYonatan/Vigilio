import { type Pagination, type UpdateDate, usePaginator } from "./usePaginator";
import { useDebounce } from "./useDebounce";
export { type Pagination, type UpdateDate };
export { useDebounce };
export default usePaginator;
